# test/

Automated tests organised to mirror `lib/` structure.

- `widget_test.dart` — Single smoke test verifying the app renders
- `core/` — Unit tests for core layer (entities, providers, strategies)
