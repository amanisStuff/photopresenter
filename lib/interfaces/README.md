# interfaces/

User-facing UI layer. Entry points, screens, and reusable widgets.

- `main.dart` — Application entry (at `lib/main.dart`, imports from here)
- `screens/` — Full-page views (PresentationScreen, SettingsScreen)
- `widgets/` — Composable UI components (ImageGrid, ImageDisplay, PresentationControls, CustomTitleBar)
