# interfaces/screens/

Full-page views that compose widgets from `widgets/` into complete screens.

- `presentation_screen.dart` — The main screen: image grid sidebar, presentation area, control bar, overlays, and drop-target for files
- `settings_screen.dart` — Configuration screen: audio mode, timer, class presets, theme selection
