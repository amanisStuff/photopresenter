# shared/

Reusable utilities, constants, and widgets shared across the app.

- `theme.dart` — AppTheme with WMP9/Luna color palette and styled BoxDecorations
- `widgets/clickable.dart` — Simple MouseRegion + GestureDetector wrapper
